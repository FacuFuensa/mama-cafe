# Polish list (final pass)

- [ ] Sin TACC badge color: currently neutral gray, should use Dulce de Leche
      (`var(--color-dulce)` background at low opacity, `dulce` text).
      Fixes lost during brand refactor when `tan` token was renamed.
- [ ] Verify all components reference brand tokens, not hardcoded hex.
- [ ] Convert photos to WebP, resize to ~1200px max.
- [ ] Add `priceRange`, `servesCuisine`, and `geo` to JSON-LD.
- [ ] Get real phone number from family, replace placeholder in BaseLayout.
- [ ] Get logo SVG from Estudio Mond (blocks hero quality).
- [ ] Fill in real prices in `menu.ts`.